package wrapperclasses;

public class MainDriver {
    public static void main(String[] args) {
        Boolean b = true; //autoboxing
        //Boolean b1 = new Boolean(true); // the line through mean it's deprecated. shouldn't be used
        Boolean b2 = Boolean.TRUE;

        Byte byt = 10; //autoboxing

        Character c = 'c'; //autoboxing

        Float f = 126F;

        char x = c; //unboxing

        /*autoboxing: the idea of having a primitive type automatically being converted to the
         correlated wrapper class*/

        /*
         * unboxing: converting a wrapper class to its primitive datatype
         * */

    }

    }

